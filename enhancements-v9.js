(() => {
  'use strict';

  const DEFINITIONS = {
    'Windows 10 与操作系统': {
      what: '操作系统（Operating System，OS）是最基本的系统软件，位于计算机硬件与应用程序、用户之间。Windows 10 是微软推出的图形用户界面操作系统。',
      use: '它负责管理处理器、内存、文件、设备等软硬件资源，为应用程序提供运行环境，同时通过桌面、窗口、菜单等界面让用户操作计算机。',
      core: ['资源管理：管理 CPU、内存、文件和外部设备。','任务管理：协调多个程序的运行。','用户接口：提供图形界面和命令入口。','服务应用：为上层软件提供文件、设备、网络等基础能力。']
    },
    '桌面、窗口与 Snap': {
      what: '桌面是登录 Windows 后看到的主要工作区域；窗口是应用程序、文件夹或系统工具在屏幕上的矩形工作区域；Snap（贴靠）是 Windows 用来快速排列窗口的功能。',
      use: '理解这三个对象，是掌握最大化、最小化、移动、调整大小和多窗口排列的基础。',
      core: ['桌面通常包含图标、快捷方式、任务栏和背景。','窗口常包含标题栏、工作区以及最小化、最大化/还原、关闭按钮。','最小化只是把窗口收起，程序通常仍在运行；关闭才是结束该窗口。','拖到屏幕边缘或顶部可触发贴靠或最大化等排列效果。']
    },
    '任务栏、开始菜单与搜索': {
      what: '任务栏是桌面上用于启动、切换和观察程序状态的区域；开始菜单是应用、设置和电源等功能的主要入口；Windows 搜索用于查找应用、文件、设置等内容。',
      use: '三者共同承担“找到功能、启动程序、切换任务”的导航作用。',
      core: ['任务栏可固定常用应用，也会显示正在运行的程序。','开始菜单提供应用列表、设置、电源等入口。','搜索不只用于找文件，也可以找应用和系统设置。','任务栏可以设置自动隐藏等显示行为。']
    },
    '文件、文件夹与路径': {
      what: '文件是以名称保存的一组数据，是操作系统管理信息的基本单位；文件夹用于组织文件和其他文件夹；路径用来描述某个对象在存储设备中的具体位置。',
      use: '理解“对象是什么、放在哪里”，才能正确完成复制、移动、删除、搜索和定位。',
      core: ['文件有名称、类型、大小、位置和属性等信息。','文件夹本身主要起组织作用，可以形成多级层次。','绝对路径从盘符或根位置开始，完整指出对象位置。','同名文件可以存在于不同文件夹中，因为路径不同。']
    },
    '文件名、扩展名与默认打开方式': {
      what: '文件名用于标识文件；扩展名通常位于最后一个“.”之后，用来表示文件类型，例如 .docx、.xlsx、.jpg；默认打开方式决定双击某种类型文件时由哪个程序打开。',
      use: '这三者分别回答“叫什么”“是什么类型”“默认用什么软件打开”，概念不能混在一起。',
      core: ['Windows 文件名不能包含 \\ / : * ? " < > | 等保留字符。','隐藏已知文件类型的扩展名只是改变显示，不会删除真实扩展名。','更改默认打开方式不会改变文件本身的数据内容。','单纯改扩展名也不等于完成真正的文件格式转换。']
    },
    '文件属性：只读与隐藏': {
      what: '文件属性是附加在文件或文件夹上的状态信息。只读属性主要表示不希望内容被直接修改；隐藏属性表示该对象在默认显示条件下可以不显示。',
      use: '属性改变的是对象的使用或显示状态，并不等于删除、加密或改变文件类型。',
      core: ['只读文件仍然存在，也通常仍可以被复制或删除。','隐藏文件仍然存在，只是显示策略发生变化。','是否显示隐藏项目由资源管理器的查看设置控制。','属性和访问权限不是同一个概念。']
    },
    '复制、移动、拖放与重命名': {
      what: '复制会产生一个副本，原对象仍保留；移动会改变对象所在位置；拖放是用鼠标完成复制或移动的一种方式；重命名只改变名称。',
      use: '这组操作的关键是观察操作前后“原位置是否还存在对象”和“目标位置是否出现对象”。',
      core: ['复制后通常原位置和目标位置都有对象。','移动后对象从原位置转到目标位置。','同一磁盘分区和不同分区的默认拖动结果可能不同。','Ctrl+拖动通常用于强制复制，Shift+拖动通常用于强制移动。','重命名不会改变文件内容。']
    },
    '删除、回收站与 Shift+Delete': {
      what: '删除是把文件或文件夹从原位置移除；回收站是 Windows 为部分本地磁盘删除对象提供的临时存放位置；Shift+Delete 是绕过回收站直接删除的常用方式。',
      use: '回收站提供“误删后恢复”的缓冲，但并不是所有删除操作都会经过回收站。',
      core: ['本地磁盘普通文件按 Delete 后通常进入回收站。','Shift+Delete 通常绕过回收站。','U 盘等可移动介质上的删除行为不能机械套用本机回收站规则。','清空回收站后，不能再通过普通“还原”按钮恢复其中项目。']
    },
    '快捷方式与快速访问': {
      what: '快捷方式是指向目标文件、文件夹或程序的链接；快速访问是文件资源管理器中的导航区域，用来显示固定或常用的位置。',
      use: '它们都是“入口”，而不是目标数据本身。',
      core: ['删除快捷方式通常不会删除它指向的原文件。','快捷方式可以放在桌面等位置。','快速访问中的项目仍存储在原来的真实位置。','把文件夹固定到快速访问不会把文件夹复制一份。']
    },
    '设置/控制面板与按钮语义': {
      what: '“设置”和“控制面板”都是 Windows 的系统配置入口；对话框中的“确定、应用、取消”用于控制设置是否生效以及窗口是否关闭。',
      use: '很多系统配置题考的不是选项名称，而是修改后究竟有没有保存、窗口有没有关闭。',
      core: ['“应用”通常保存当前修改，但不关闭对话框。','“确定”通常保存修改并关闭对话框。','“取消”通常放弃尚未应用的修改并关闭窗口。','控制面板可按类别、大图标、小图标等方式查看。']
    },
    '任务管理器': {
      what: '任务管理器是 Windows 用来查看和管理正在运行的应用、进程以及系统资源使用情况的工具。',
      use: '程序无响应、需要查看 CPU/内存占用或管理启动项时，经常会用到任务管理器。',
      core: ['可查看正在运行的应用和进程。','可结束无响应任务。','可查看 CPU、内存、磁盘、网络等性能信息。','可管理部分启动项并定位程序文件位置。','它不是常规的软件卸载工具。']
    },
    '设备管理器与驱动程序': {
      what: '设备管理器用于查看和管理计算机硬件设备；驱动程序是操作系统与具体硬件之间的接口软件，使系统能够正确识别和控制设备。',
      use: '硬件无法正常工作时，可以通过设备管理器查看状态、驱动和错误标记。',
      core: ['设备旁出现黄色感叹号通常表示设备或驱动存在异常。','可以查看设备属性、更新或回退驱动、启用或禁用设备。','驱动程序不是硬件本身。','设备管理器负责设备管理，不等同于任务管理器。']
    },
    '系统还原、睡眠与关机': {
      what: '系统还原用于把系统文件、注册表、驱动和部分配置恢复到以前的还原点；睡眠是低功耗保留当前工作会话；关机则结束系统会话并关闭计算机。',
      use: '三者解决的问题不同：系统还原解决系统配置回退，睡眠解决暂时离开，关机解决结束本次使用。',
      core: ['系统还原不是个人文件备份。','睡眠通常保留当前打开的程序和工作状态。','正常关机应通过系统的电源命令完成。','长按电源键属于异常情况下的强制断电手段，不应作为正常关机方式。']
    },
    '记事本': {
      what: '记事本是 Windows 自带的纯文本编辑器，主要用于创建和编辑不含复杂排版信息的文本文件。',
      use: '它适合记录纯文字、查看配置文本或临时编辑内容，不适合制作 Word 那样的富文本文档。',
      core: ['可以输入、复制、剪切、粘贴和查找纯文本。','可以设置整体显示使用的字体、字形和字号。','不能像 Word 那样让不同文字分别拥有复杂颜色、段落和图片排版。','从 Word、网页或 Excel 复制文字到记事本时，通常只保留纯文本内容。']
    },
    '截图工具与画图': {
      what: '截图工具用于截取屏幕上的区域、窗口或全屏画面；画图是 Windows 的基础位图绘制和图像编辑工具。',
      use: '前者重点是“获取屏幕图像”，后者重点是“对图像做基础编辑”。',
      core: ['截图工具可截取并保存屏幕画面。','画图可进行绘制、裁剪、调整大小等基础操作。','两者功能范围有限，不应把专业图像处理能力强行套给它们。','例如“自动识别并删除水印”不是截图工具的常规功能。']
    },
    '剪贴板与 U 盘策略': {
      what: '剪贴板是内存中的临时数据交换区域，复制或剪切内容后，应用程序可从剪贴板粘贴；U 盘策略则属于对可移动存储设备读写行为的系统管理。',
      use: '这两个知识点都涉及“数据怎样在系统中临时保存或受权限控制”，但一个是数据交换机制，一个是设备访问策略。',
      core: ['复制会把数据副本放入剪贴板；剪切也会把待移动内容放入剪贴板。','睡眠与关机对内存状态的影响不同，不能把二者混为一谈。','U 盘等可移动存储设备的读写可受系统策略限制。','本地组策略等管理工具可用于配置部分可移动存储访问规则。']
    },

    'Word 2016 视图与显示比例': {
      what: '视图是 Word 对同一份文档采用的不同显示方式；显示比例只是把屏幕上的文档放大或缩小观看。',
      use: '视图帮助完成不同任务，例如排版、阅读、调整标题层级；缩放只影响观看大小，不改变文档真正字号。',
      core: ['页面视图接近打印后的页面效果。','阅读模式强调阅读体验。','大纲视图按标题层级组织文档结构。','Web 版式以连续网页式布局显示。','把显示比例从 100% 调到 150% 不会把 12 磅字体真正变成更大的字号。']
    },
    '字符格式': {
      what: '字符格式是直接作用于所选文字字符的外观属性，例如字体、字号、字形、颜色、下划线、删除线和字符间距。',
      use: '它控制“字长什么样”，与控制整段布局的段落格式不同。',
      core: ['先选中文字，再设置字符格式。','字体是字形设计，字号是文字大小，字形常见有常规、加粗、倾斜等。','下划线可以有不同线型和颜色。','字符格式可以通过清除格式或样式等方式统一管理。']
    },
    '段落格式、缩进与间距': {
      what: '段落是以段落标记结束的一段文本。段落格式控制整段文字在页面中的布局，包括对齐、缩进、行距、段前和段后间距。',
      use: '如果问题描述的是“整段往哪边、第一行从哪里开始、行与行距离多大”，通常属于段落格式。',
      core: ['对齐包括左对齐、居中、右对齐、两端对齐、分散对齐。','首行缩进只让第一行向内缩；悬挂缩进让除第一行外的其他行向内缩。','左/右缩进是段落级设置，不等于页面页边距。','行距控制段内各行距离；段前/段后控制段落之间距离。']
    },
    '中文版式高级设置': {
      what: '中文版式高级设置是 Word 针对中文与西文、数字等混排时提供的一组排版规则，例如自动调整中文与西文间距、中文与数字间距以及换行规则。',
      use: '它们会影响一行能容纳多少字符，因此即使文字和字号不变，也可能改变换行位置和段落占用行数。',
      core: ['中文与西文间距是中文字符和英文字母交界处的自动间距。','中文与数字间距是中文字符和阿拉伯数字交界处的自动间距。','关闭或开启这些规则可能让同一段文字从三行变成两行，或反过来。','它属于版式规则，不等同于手工插入空格。']
    },
    '样式与格式刷': {
      what: '样式是一组已经命名的字符和段落格式集合，例如“标题1”“正文”；格式刷用于把一个位置的格式复制到另一个位置。',
      use: '样式适合统一管理大量相同层级内容；格式刷适合快速复制局部格式。',
      core: ['修改一个样式，可以让所有使用该样式的内容同步更新。','自动目录、导航窗格等结构功能也依赖标题样式或大纲级别。','格式刷复制的是格式，不是文字内容。','样式比逐个手工修改更适合长文档。']
    },
    '查找与替换': {
      what: '查找用于定位文档中的文字、格式或特殊内容；替换在查找基础上，把匹配内容批量替换成新的文字或格式。',
      use: '它不仅能改文字，也能完成“文字不变、只改格式”的批量处理。',
      core: ['查找可以按文字内容定位。','高级查找还可以指定格式和特殊字符。','替换可以只替换文字、只替换格式，或文字和格式同时替换。','“全部替换”会一次处理所有符合条件的匹配项，使用前应确认条件。']
    },
    '分页符、分节符与页面设置': {
      what: '分页符只强制后续内容从下一页开始；分节符把文档分成可以分别设置版式的“节”；页面设置控制页边距、方向、纸张大小等页面级属性。',
      use: '想“换页”用分页思维；想让前后部分拥有不同页眉、页码、方向或分栏时，需要分节思维。',
      core: ['分页符不创建新的独立节。','下一页分节符既开始新节，也从下一页开始。','连续分节符创建新节但不一定换页。','某一页单独横向时，通常要用分节符把它与前后页面隔开。']
    },
    '页眉、页脚、页码与“链接到前一节”': {
      what: '页眉和页脚是页面顶部、底部重复出现的独立区域；页码通常作为自动字段插入；“链接到前一节”决定当前节的页眉页脚是否沿用上一节。',
      use: '当不同章节需要不同页眉、页脚或页码体系时，必须先有“节”，再控制是否链接上一节。',
      core: ['新节的页眉页脚可能默认仍与前一节相连。','关闭“链接到前一节”后，当前节才可以独立修改。','首页不同、奇偶页不同是页眉页脚的另外两类设置。','页码可以设置位置、格式和起始编号。']
    },
    '分栏': {
      what: '分栏是把正文文本按报纸式布局流入两栏或多栏的页面排版功能。',
      use: '它适合制作报刊、简报等多栏正文，文本会自动从一栏流到下一栏。',
      core: ['可设置一栏、两栏、三栏以及偏左、偏右等布局。','可以设置栏宽和栏间距。','分栏不是插入多个独立文本框。','若只让文档某一部分分栏，通常需要用分节控制范围。']
    },
    '图片、环绕与对象排列': {
      what: '图片插入 Word 后属于文档对象；文字环绕决定正文怎样绕过图片；对象排列控制图片、形状等对象的前后层次、位置、对齐和组合。',
      use: '图片“放在哪里”与文字“怎样绕着它走”是两个相关但不同的问题。',
      core: ['嵌入型图片像一个大字符，跟随文字行移动。','四周型、紧密型、穿越型、上下型等属于不同环绕方式。','衬于文字下方和浮于文字上方控制对象与文字的层次关系。','调整大小改变对象尺寸；裁剪只保留图像的一部分，两者不能混为一谈。']
    },
    '表格': {
      what: 'Word 表格是由行、列和单元格组成的结构化排版对象，用来整齐组织文字和数据。',
      use: '它不仅能排版，还支持排序、简单公式、跨页重复标题行等文档处理。',
      core: ['可插入或删除行列、合并或拆分单元格。','可设置行高、列宽、边框、底纹和单元格对齐。','Word 表格也可以排序。','可使用 SUM(ABOVE)、SUM(LEFT) 等简单公式。','跨页表格可以设置重复标题行。']
    },
    '题注与交叉引用': {
      what: '题注是给图片、表格、公式等对象自动添加“标签 + 编号 + 说明”的功能，例如“图1 泰山石”；交叉引用是在正文中插入对这些编号对象的动态引用，例如“如图1所示”。',
      use: '它们让长文档中的编号可以自动维护，前面新增或删除对象后不必手工逐个改“图1、图2”。',
      core: ['题注可选择“图”“表”等标签并自动编号。','交叉引用可以只引用标签和编号，也可以引用完整题注等内容。','手工键入“图1”只是普通文字，不会自动随编号变化。','编号变化后可以通过更新域刷新交叉引用。']
    },
    '脚注、尾注、批注与题注': {
      what: '脚注、尾注、批注、题注是四种用途完全不同的文档对象：脚注用于当前页底部的补充说明，尾注位于节末或文档末，批注用于审阅评论，题注用于给图表等对象编号说明。',
      use: '辨别它们时不要只看“都是附加文字”，要看文字出现在哪里、服务什么对象。',
      core: ['脚注通常显示在当前页底部。','尾注通常集中在节末或文档末。','批注属于审阅意见，不直接成为正文。','题注附着于图、表、公式等对象的编号说明。']
    },
    '目录与标题层级': {
      what: '自动目录是 Word 根据文档中的标题样式或大纲级别，自动提取标题文字和页码生成的文档结构列表。',
      use: '使用自动目录后，标题或页码变化时可以统一更新，而不需要手工修改一长串页码。',
      core: ['先正确设置标题层级，再生成自动目录。','“标题1、标题2、标题3”通常对应不同层级。','目录中的页码和标题可以更新。','手工输入的一串“标题……页码”不具备自动目录的联动能力。']
    },
    '邮件合并': {
      what: '邮件合并是把一份固定格式的主文档与一份包含姓名、地址等变化数据的数据源结合，批量生成多份个性化文档的功能。',
      use: '适合批量制作邀请函、通知书、信封、标签等“格式相同、对象不同”的文档。',
      core: ['主文档保存固定文字和版式。','数据源保存每个收件人的字段数据，可来自 Excel 等。','合并域是插入主文档中的变量占位符，例如 «姓名»。','“完成并合并”后根据每条数据记录生成对应结果。']
    },
    '修订': {
      what: '修订（Track Changes）是 Word 的审阅功能，用来记录对文档的插入、删除和格式修改，使其他人能够看到修改痕迹并逐项接受或拒绝。',
      use: '它适合论文、合同、报告等多人协作审阅场景。',
      core: ['开启修订后，后续修改会被记录。','接受修订会把修改正式并入文档。','拒绝修订会放弃该项修改。','修订记录和批注不是同一种对象。']
    },
    '批注与文档比较': {
      what: '批注是附加在某段文字或位置上的审阅意见；文档比较用于比较两个文档版本，并识别它们之间的插入、删除和格式差异。',
      use: '批注适合“讨论”；比较适合“找两个版本哪里不一样”；它们都与修订有关联，但不是同一个功能。',
      core: ['批注不必直接改正文内容。','删除批注不会自动删除被评论的正文。','文档比较需要原文档与修订后的文档两个版本。','比较结果可以集中显示两版差异。']
    },
    '打印与文档输出': {
      what: '打印与输出是把 Word 文档转换成纸面或其他可分发形式的最后阶段，包括打印预览、打印范围、份数、打印机以及 PDF 等输出。',
      use: '在真正输出前通过预览检查分页、页眉页脚、方向和内容范围，可以避免版式错误。',
      core: ['打印预览用于检查最终页面效果。','可以指定全部页、当前页或自定义页码范围。','打印份数和打印机属于输出设置。','另存为或导出 PDF 可保留较稳定的页面版式。']
    }
  };

  const $ = (s, r=document) => r.querySelector(s);
  const $$ = (s, r=document) => [...r.querySelectorAll(s)];
  const esc = s => String(s).replace(/[&<>"']/g, ch => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));

  function conceptBlock(def) {
    return `<section class="v9-concept-note" aria-label="概念笔记">
      <div class="v9-note-part"><span>是什么</span><p>${esc(def.what)}</p></div>
      <div class="v9-note-part"><span>有什么用</span><p>${esc(def.use)}</p></div>
      <div class="v9-note-part"><span>基本组成与规则</span><ul>${def.core.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>
    </section>`;
  }

  function convertTrapLab(lab) {
    if (!lab || lab.dataset.v9Converted) return;
    lab.dataset.v9Converted = '1';
    const items = $$('button[data-truth]', lab).map(btn => ({
      statement: btn.querySelector('span')?.textContent?.trim() || '',
      why: btn.dataset.why || '',
      truth: btn.dataset.truth === 'true'
    }));
    if (!items.length) return;
    lab.className = 'v9-pitfall-note';
    lab.innerHTML = `<details>
      <summary>补充：易错点（概念看懂后再看）</summary>
      <div class="v9-pitfall-list">${items.map(it => `<div><b>${it.truth ? '正确表述' : '错误表述'}</b><p>${esc(it.statement)}</p><small>${esc(it.why)}</small></div>`).join('')}</div>
    </details>`;
  }

  function upgradeCourseModule(module) {
    if (!module || module.dataset.v9ConceptFirst) return;
    const title = $('.module-head h2', module)?.textContent?.trim();
    const def = DEFINITIONS[title];
    if (!def) return;
    module.dataset.v9ConceptFirst = '1';

    const head = $('.module-head', module);
    if (head) {
      const small = $('small', head);
      if (small) small.textContent = '知识点笔记';
      const lead = $('p', head);
      if (lead) lead.classList.add('v9-summary-line');
      head.insertAdjacentHTML('afterend', conceptBlock(def));
    }

    const cardTitle = $('.knowledge-card h3', module);
    if (cardTitle) cardTitle.textContent = '进一步掌握：规则与操作';
    convertTrapLab($('.truth-lab', module));

    const special = $('.special-title', module);
    if (special) {
      const s = $('span', special), b = $('b', special);
      if (s) s.textContent = '理解演示';
      if (b) b.textContent = '通过变化直接看懂概念';
    }
  }

  function labelExistingConceptBodies() {
    $$('.concept-copy').forEach(copy => {
      if (copy.querySelector(':scope > .v9-existing-note-label')) return;
      const body = copy.querySelector(':scope > .note-body');
      if (!body) return;
      const label = document.createElement('div');
      label.className = 'v9-existing-note-label';
      label.textContent = '概念与作用';
      copy.insertBefore(label, body);
    });

    $$('.lab-heading > div > p').forEach(p => {
      if (/操作观察/.test(p.textContent)) p.textContent = p.textContent.replace('操作观察', '理解演示');
    });
  }

  function deQuizExamAddons() {
    // Keep useful examples and visual results, but remove answer-picking UI.
    $$('.exam-options').forEach(el => el.remove());
    $$('.exam-deep-dive .feedback, .reference-deep-dive .feedback').forEach(el => el.remove());

    $$('.reference-deep-dive .screen-tabs button').forEach(btn => {
      btn.textContent = btn.textContent
        .replace('② 2026 排错', '② 引用漂移示例')
        .replace('③ 2020 RANK', '③ RANK 排名示例');
    });
    $$('.reference-deep-dive h4').forEach(h => {
      h.textContent = h.textContent
        .replace(/2026 年真题第 59—60 题/g, '例：向下填充造成引用漂移')
        .replace(/第 59 题：可能产生问题的原因是？/g, '为什么会出错')
        .replace(/第 60 题：哪一种解决方案可行？/g, '正确修正方式')
        .replace(/2020 年真题：名次为什么错？/g, '例：RANK 排名区域为什么要固定');
    });
    $$('.exam-head > span').forEach(s => {
      if (/真题|高频/.test(s.textContent)) s.textContent = '补充示例';
    });
    $$('.exam-head > p').forEach(p => {
      p.textContent = p.textContent.replace(/\d{4}[、，]?/g, '').replace(/真题/g, '实例');
    });
  }

  function normalizeHeroLanguage() {
    $$('.course-hero small').forEach(el => el.remove());
    const meta = $('meta[name="description"]');
    if (meta && /交互练习|随学随测/.test(meta.content)) meta.content = '山东专升本计算机详细交互笔记：概念、规则、操作与可视化理解。';
  }

  function apply() {
    $$('.course-module').forEach(upgradeCourseModule);
    labelExistingConceptBodies();
    deQuizExamAddons();
    normalizeHeroLanguage();
  }

  let queued = false;
  function schedule() {
    if (queued) return;
    queued = true;
    requestAnimationFrame(() => { queued = false; apply(); });
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', schedule, {once:true});
  else schedule();
  document.addEventListener('notes:chapterchange', schedule);
  new MutationObserver(schedule).observe(document.documentElement, {childList:true, subtree:true});
})();
