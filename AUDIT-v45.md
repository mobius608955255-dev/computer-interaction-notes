# v45 · 全站阅读、查找与模型结构改版

## 覆盖范围

- 保留11章、220个笔记ID、460道真题来源，以及全部938个原有要点。与v44逐字段比对，除关联和分组元数据外原正文未改变。
- 全部220条笔记添加有阅读目的的关联，共530条。90条较长笔记按子问题分组，每个要点恰好出现一次。
- 13个易混知识对照主题、67行对照，每行可回看原文。既有46张笔记内对照表继续保留。
- 首页全站搜索覆盖正文、边界、原表格、来源及别名和对照主题。检索不读取演示中的可变输入；结果直达相关段落。1644个正文段落锚点全部存在。
- 详读和快速复习使用同一DOM：快速复习保留结论与边界，完整推理可单独展开。切换模式不重建演示；偏好仅保存在当前浏览器。来自搜索的深链接自动展开对应细节。

## 修复连续操作

1. PPT重命名节固定打开时的目标，并禁用背景控件、在状态层拒绝改变目标或页面结构的动作；取消保留原名。
2. PPT翻页后结束放映，以当前页恢复编辑，同时同步所属节、选择范围和切换效果。
3. Excel高级筛选的复制旁表在切换自动/高级筛选及清除源表筛选后保留；明确重新复制或重置才改变副本。
4. 从对照锚点发起搜索时移除旧锚点，刷新或分享URL能恢复查询。异步载入只呈现最新查询，清空和失败重试均有回归。

## 代码结构

- 将按年份/补丁文件混合加载的133个模型（134个笔记ID，含一组别名）迁移到10个章节领域。第9章继续使用原有概念场景。
- 每章只登记本章模型；公共计算集中在shared/math.js。build从manifest按确定顺序拼接静态演示包。
- 一次性迁移用AST保留词法依赖、IIFE与语句顺序；逐一核对初始状态、初始渲染、注册元数据及所有注册外钩子。正式构建无新增依赖、AST或动态模块解析。
- 移除10个旧模型文件，避免两套维护入口；Word页码和表格两条必要包装链保留原闭包与顺序。
- 将阅读视图、首页发现、纯搜索和派生索引分别独立成模块。运行时不再直接判断Word页眉或PPT黑屏的模型ID。
- 网络传输、排练计时和切换/动画三个计时模型以frameKey/patchFrame指定动态区域，保留稳定控件节点和焦点。布局跨页或结束时完整渲染。
- 每章6—7个脚本，未压缩JS约200—405KB，相比v44减少19%—38%。此为静态字节量比较，不代表实测下载或执行时间。

## 验证

- 自动化行为回归：144项，覆盖全部卡片打开/复位/收起、各初始可见操作、计算边界和本轮新行为。
- Word/Excel/PPT各3条复杂连续操作链另行检查，包含未blur输入、确定/取消、作用范围、重复操作、收起再开和重置。
- 对全部笔记逐条校验段落存在、938个要点无遗漏、关联目标有效、每章注册集合精确。
- 浏览器：360/390像素预览框（实际内容345/375像素），快速复习和详读两模式，11章共880次卡片检查；未发现正文横向溢出或演示控件裁切。
- 浏览器人工确认首页搜索、段落跳转、对照展开与水平滚动、原文链接和手机阅读控件；桌面入口同样验证。
- 静态生成物随源码提交，GitHub Actions重新生成并检查一致性，再由原GitHub Pages站点发布。

范围限制：窄屏浏览器验证不等于全部Android机型实测；教学演示仍是知识点的局部模型，并非完整Office复刻。
