# 网络、媒体、安全、数据库与算法精修（v37）

范围：教材第6—11章共78条笔记；精修18条正文、增加17张概念对照表、重建10个独立演示。原有2020—2026年460道题仍归入220条笔记和11个教材章节。

## 具体改进

- 第6章：链路瓶颈与有效传输进度，基准URL与相对路径，IPv6展开与规范缩写。已有DNS、HTTPS、IPv4及可编辑HTML模型保留。
- 第7章：采样点与量化档位可视化、PCM大小计算、可逆游程编码与先减色造成的信息损失。
- 第8章：有序访问控制规则实际匹配，鼠标/触屏拖动改变规则次序；同一个来源地址贯穿请求与日志。Web Crypto实际执行ECDSA签名验签、RSA-OAEP加解密、SHA-256摘要。
- 第9章：释放云实例后禁用扩容，清除已释放资源显示，保持计费为零，恢复样例后才可再次操作。
- 第10章：可编辑关系输入、所有同名字段自然连接、投影实际去重；SQL逐次修改同一份表数据，DROP后后续查询报表不存在。
- 第11章：可输入数组与升降序，逐次比较、逐趟与完整排序；保留相等值的原位置以检查稳定性。

## 验证与范围

- 97项回归检查通过，包含全部章节卡片挂载/重置与以前的Windows、Word、Excel、PowerPoint检查。
- 新增检查覆盖无效IPv6/CIDR、字节与完整采样点、真实密码运算、规则拖动、自然连接、NULL、连续SQL、精度拒绝、DROP后状态及排序稳定性。
- 不使用网络请求进行教学计算。密码模型依赖支持Web Crypto的安全页面环境，私钥只在本次卡片中保存；重置卡片后重新生成。
- SQL是范围明确的内存子集，界面列出支持语句与字段名，拒绝不支持的语法和超出精度的数值；学习撤销不表示真实数据库所有操作都可撤销。
- 防火墙采用首条匹配的教学规则，不冒充Windows防火墙优先级；传输为固定吞吐、单次启动等待模型；波形为低频教学图，游程为编码原理示例，不冒充媒体文件编码器。

## 本轮正文清单

| 知识点ID | 精修主题 |
|---|---|
| y2020q26 | 数字网络的带宽表示数据传送能力，吞吐量表示给定时间内实际通过的数据速率 |
| merged-13 | OSI参考模型自下而上有物理、数据链路、网络、传输、会话、表示、应用七层 |
| y2023q17 | 传统C类IPv4网络默认掩码为255.255.255.0，即/24 |
| y2023q47 | IPv6地址固定为128 bit，即16 B |
| y2026q14 | 典型HTTP或HTTPS URL可分为方案、主机、端口、路径、查询和片段 |
| y2026q28 | 查全率看全部相关文献找回了多少，查准率看检出文献中有多少相关 |
| y2020q27 | 多媒体包含文本、图形、图像、音频、动画和视频。位图由像素组成 |
| y2024q32 | 未压缩 PCM 音频的数据量由采样率、量化位数、声道数和时长共同决定 |
| merged-14 | 无损压缩保证解码后恢复被编码的数据 |
| y2023q18 | 流媒体的核心是边接收边播放，无须等整个文件下载完成 |
| merged-15 | 加密用于保密，数字签名用于核验签署来源与数据完整性，摘要用于检测数据是否改变。三者目标不同 |
| merged-16 | 防火墙按规则控制经过它的网络流量。判断允许还是阻止，要把同一请求的方向、协议、地址和端口等条件一起代入规则，再考虑未匹配时的默认动作。 |
| y2023q14 | 关系的一行称元组，一列对应属性，属性允许取值的集合称域 |
| merged-20 | 选择筛选行，投影取得列并按关系代数的集合语义去重，连接按匹配条件组合元组 |
| merged-21 | SELECT指定结果列，FROM指定数据来源，WHERE筛选行，ORDER BY规定输出顺序 |
| y2024q36 | DELETE FROM student WHERE 班级='一班'删除符合条件的记录并保留表结构 |
| y2024q69 | 本例从左向右进行升序冒泡：仅在a[j]＞a[j+1]时交换相邻元素，一趟后本趟最大值到右端 |
| y2021q33 | 本例先执行S=S+k，再令k=k+3，最后判断k＜10 |
