# 内容升级第一轮：2.3 文件与文件夹管理

## 恢复入口

基线：`c1812aa4711bf3752adc165d82c992f67fab4385`，检查时 Git 工作区、未暂存及暂存差异均为空。没有回退或覆盖上一轮成果。`npm run check` 基线 172 项通过。旧的完成记录只作线索，本轮重新读取了代码和相关来源。

状态：本轮内容与代码验收完成。2.3 从 6 个单元、29 个正文点扩为 9 个单元、74 个正文点，补齐路径、选择与新建、搜索和高级属性的学习流程。全部原 ID、题源关联、keys 数组和原有段落编号保留；新细节追加或修正原段落，没有迁移个人数据。其他章节正文逐文件与基线比对一致。

采用山东省 **2026 年**考试要求的计算机部分（印刷页 15—19）；其中页 19 仅说明 2027 年软件版本不变，不能称已核查 2027 年完整考纲。核验环境基线为 Windows 10 / Office 2016；网站运行环境并非真实 Windows 或 Office。

## 范围与资料

试点选取已上课且原始资料充分的 2.3，包含概念和路径、命名和属性、选择和基本操作、搜索、ZIP、回收站、导航入口；剪贴板为直接依赖。其他章节不扩写。课堂及原卷逐点来源、实际读取范围、答案争议保存在私有的《计算机笔记_内容升级第一轮_内部核验记录》中；公开仓库只包含重新组织的知识解释和公开标准链接，不上传原始课件、转写、个人测验及其摘录。

## 后续顺序

本轮验收后停止。下一批先处理 Word 2016 的文本/段落与分页分节，入口为第 9—12 次课资料包及对应实际内容的转写；再处理 Excel 2016 工作表、输入与引用。先复核来源内容再确定范围，不按转写编号猜测课次；不继承未核实的题量、频率和全站覆盖率。

## 实际读取与内容验收

已打开全部 11 个课次 ZIP 并列出内部文件，确认是课件图片。2.3 的连续完整课件范围为第6次课幻灯片8、第7次课幻灯片9—10，均看原图，并看相邻边界页及快捷方式相关页。课堂转写6/7/8全文已读；3的相关前置范围、4的快捷方式范围另行读取。文件编号不等于课次编号。

2020—2026 七份题卷共81页均按原图筛查，试点相关题干、全部选项与对应答案另作细读。配套答案均缺少可确认的官方发布依据。第2章填空练习3页全文已读，相关前2页核图。7份AI参考稿已读取有关完整范围，角色仅为待核验候选；软件争议结合Microsoft一手资料核验。逐文件的页码、时间戳、失败及范围均在私有来源记录中，不将全页筛查表述为全部题目审计通过。

51项课堂试点/直接依赖候选已有对应正文；另1项相邻2.2的快捷方式对象枚举留后续。这是限定来源候选的映射，不是全章或全站覆盖率。逐题映射另按选项记录，不能与课堂候选相加冒充考点总数。

主要改进：新增路径、库、选择/新建/改名、搜索范围与通配符的完整解释；补复制/移动/回收站操作链、常规/高级属性、EFS前提、共享与权限层次。保留原本正确的同卷/跨卷拖放、扩展名不转换格式、ZIP提取和文件剪切待移动规则。修复ZIP连续操作擅自改回名称及同名冲突、属性演示高亮错对象和非真实切换；移除对应旧场景及死分支。来源争议放在正文后，搜索新增具体段落跳转。

## 代码与页面验收

- 基线 `npm run check`：172/172通过。最终构建成功（236条笔记，460个原题源标识，11章）；最终 `npm test`：177/177通过，约49秒，无跳过项。没有独立typecheck/lint命令，JavaScript语法与引用通过构建、行为测试及 `node --check`、`git diff --check` 核验。
- 新回归覆盖实际Ctrl/Shift混选、重复/非法名称拒绝、路径上级/历史、搜索范围与模式、ZIP连续操作、属性独立状态、搜索段落锚点。初次新测试暴露测试定位器未转义反斜杠、JSDOM原生锚点跳转需等待事件轮次；均修正测试基础用法并保留断言，没有降低预期。
- Chrome实际打开试点。9张卡片逐一在360/390/1200px预览框检查，实际内容视口345/375/1185px，正文横向溢出均0；360px的本章28张卡片控件裁切检查为空。桌面与窄屏截图已目视核对。
- 浏览器验证目录跳转、EFS搜索→具体段落、清空恢复、扩大操作区→保留选区返回、真实Ctrl/Shift单击、文件名搜索范围、文件只读与目录权限独立、ZIP先改名后打包，以及鼠标真实拖入目标后源/目标改变。长按的触发、轻点不触发、取消手势已由现有自动化指针/计时回归验证；没有物理手机长按实测。
- 无本站脚本错误或资源加载错误；浏览器扩展自身的元数据报错与站点脚本区分记录。共享的正文、来源、目录、搜索索引与演示包由同一构建生成，未新建平行网站。

## 关键文件

正文与公开依据：`content/chapter2.json`、`content/references.json`。模型：`src/labs/chapter2/windows.js`、`file-management.js`、`manifest.json`。旧场景清理：`content/legacy-demos.json`、`simulations.js/css`、`notes-app.js`。阅读和布局：`notes-chapter-search.js`、`notes-learning.css`、`note-labs.css`。回归与规范：`tests/notes-regression.cjs`、`README.md`、本记录；版本49与生成物同步更新。其他章HTML/数据包的变化是共用版本号及资源摘要更新，不是正文扩写。

## 待核验和停止边界

- 2023第33题：扫描题面与未署名参考答案矛盾，保留正确格式机制和争议，不擅自替答案定性为官方。
- 2026第25题：“此电脑”特定对象的拖到桌面、发送到菜单未作原生Win10核验；来源区已明示。系统图标设置恢复路径已有官方依据，不能因此宣称该题所有选项都已闭环。
- Windows 10、Office 2016真实软件均未实测；网页模拟器的结果不能替代实机证据。ZIP示例命名、模式匹配和属性模型均说明了模拟范围。
- 2.4移动存储策略工具入口、任务栏边界，2.6附件细节和2.2旁支列为后续，不扩大本轮。没有资料读取失败阻断本试点；原始月考/新版完整考纲在本次可访问清单中未定位，未要求补传全部资料。

打开原网站的 `chapter2.html?v=49#syllabus-windows-paths`，或在目录中进入2.3。原GitHub Pages项目继续发布，不上传课堂原始材料。记录不是验收替代品；下次恢复仍先核对Git差异、内容哈希与实际页面。
