/* Standards corrections are applied after all years have merged into chapter notes. */
(() => {
  const notes=window.NOTES.notes;
  const change=(id,values)=>Object.assign(notes.find(n=>n.id===id),values);
  const add=(id,...points)=>notes.find(n=>n.id===id).points.push(...points);
  change('merged-3',{conclusion:'bit是二进制位；1 Byte=8 bit。容量单位要区分十进制SI前缀与二进制IEC前缀。',points:['SI：1 kB=1000 B，1 MB=1000 kB，1 GB=1000 MB，1 TB=1000 GB。','IEC：1 KiB=1024 B，1 MiB=1024 KiB，1 GiB=1024 MiB，1 TiB=1024 GiB。','旧教材常把KB、MB等用于1024进位；做容量计算时先读清题设约定，标准记法用KiB、MiB等区分。','像素是数字图像的基本图像元素；bit/s表示位速率，Byte表示字节量，b与B不能混写。'],boundary:'跨级换算要逐级相乘；未压缩图像数据量约为像素数×每像素位数，再除以8换成字节。'});
  add('merged-5','Web版式和草稿视图不呈现正常的分页、页眉页脚版式；核对纸张上的效果应切到打印布局。');
  add('merged-7','上标快捷键为Ctrl+Shift+=；下标为Ctrl+=。字体对话框还可设置单/双下划线、删除线和着重号，它们与校对波浪提示不同。');
  add('merged-8','自定义项目符号可选符号或图片；符号项目可设置字体及颜色。编号可改样式和起始值，不需要手动逐项改数字。');
  add('merged-10','RANK.EQ(number,ref,[order])：order为0或省略时降序，非0时升序；相同数值同名次，后续名次会跳号。固定比较区域，如=RANK.EQ(B2,$B$2:$B$7,0)。');
  add('merged-12','系列对应图例中的各组数据；类别通常对应横轴标签。“按系列”是一整组依次出现，“按类别”是同一横轴类别下的各系列一起出现。');
  add('merged-17','数字签名使用非对称密码：签名者用私钥签名，接收方用相应公钥验证。哈希摘要本身不等于数字签名；签名用于验证来源与完整性，不等于加密内容保密。');
  add('merged-19','常用三层架构的完整名称：感知层、网络层、应用层。传感器/RFID归感知层，传输归网络层，具体服务归应用层。');
  change('y2026q28',{boundary:'布尔运算优先级取决于检索平台，例如PubMed按从左向右处理；复杂表达式用括号明确组合，不背统一的默认顺序。'});
  change('y2026q38',{conclusion:'br是换行空元素，没有结束标签；p是段落元素，并非空元素。HTML允许在规定条件下省略p的结束标签。'});
  add('y2026q6','辗转相除法中，若本轮a除以b余数为0，最大公约数就是当前除数b；首轮整除时也适用。输入域和终止条件必须明确。');
  change('y2026q50',{points:['SUMIF(range,criteria,[sum_range])：先放条件区域，求和区域可省略；包含“北斗”的文字条件写"*北斗*"。','SUMIFS(sum_range,criteria_range1,criteria1,...)：求和区域在最前，所有条件必须同时成立；条件区域须与求和区域尺寸相同。','SUMIF也建议条件区域与求和区域同尺寸；若不同，Excel会从sum_range左上角起按条件区域尺寸确定实际求和范围，不应笼统说必报错。','交叉表纵横复制时，用混合引用分别固定公司列与季度标题行。']});
  add('y2025q52',"跨年度比较：=$D3>VLOOKUP($A3,'2023'!$A$2:$C$22,3,0)。先选当前表的完整数据行范围，再创建公式规则并设为加粗、倾斜；$D3与$A3锁列放行，历史查找区全锁。");
  add('y2025q53','更改节点为圆角矩形：选中目标形状边框→SmartArt工具→格式→更改形状→圆角矩形。');
  add('y2025q2','常见GB2312双字节汉字机内码的两个字节最高位均为1，但满足这点仍不保证码位有效。','字形可用点阵或轮廓描述；同一汉字在不同字体、字号下的字形数据不唯一，字形数据也不是字符编码。');
  add('y2025q33','传统Windows截图按键：Print Screen复制全屏，Alt+Print Screen复制活动窗口，结果进入剪贴板；若用户重新配置了按键行为，则按实际设置执行。');
  add('y2025q34','本章以Windows 10传统记事本为准：保存纯文本，不保存逐字颜色、表格边框等富文本格式。');
  add('y2020q27','多媒体系统包括支持采集、处理、存储、输出的硬件与软件；声音等模拟信号进入计算机需数字化。','MP3是一类有损音频编码；MP4是多媒体容器，可承载视频、音频等，不应仅凭扩展名判断是否只有声音或是否无损。');
  add('y2023q10','页面设置可指定起始页码；工作表页可选“先列后行”或“先行后列”的打印顺序。','同一工作表可定义多个不连续打印区域，通常分别打印；清除打印区域会恢复按工作表内容确定的范围。');
  const timing=notes.find(n=>n.id==='y2023q13');timing.points=timing.points.map(p=>p.includes('摄像头')?'Office 2016录制放映可记录旁白、墨迹和计时；不要套用新版摄像头录制界面。':p);
  change('y2024q8',{title:'Word表格的转换、计算、排序与删除'});
  add('y2024q8','插入→表格→文本转换成表格：用制表符、逗号等分隔列，用段落标记分隔行。','表格工具→布局→数据→排序，可按文字的拼音或笔划等方式排序；按题意指定关键字、类型和升降序。','在结果单元格使用表格工具→布局→公式，如=SUM(ABOVE)求上方连续数字的和。修改源值后可更新域，不能假定总像Excel一样实时重算。');
  change('y2026q47',{title:'中文与数字、中文与西文的自动间距分别控制',conclusion:'中文与数字、中文与西文是两个独立选项。文段含“10亿年”等数字时，应检查“自动调整中文与数字的间距”。',points:['入口：开始→段落对话框启动器→中文版式→字符间距。','取消中文与数字间距，影响汉字和数字交界处；中文与西文选项影响汉字与拉丁字母交界处。','自动间距不同于手动输入的空格；关闭自动间距不会删除真实空格，也不会改字号。','行数是否减少还取决于字体、字号和行宽，不能保证每个文段都恰好从三行变两行。']});
  change('y2026q51',{title:'筛选隐藏记录，“清除”恢复全部数据',trigger:'原位置高级筛选后恢复全部记录；按职称做自动筛选时辨认另一种入口。',points:['高级筛选在原区域显示结果时，会隐藏未命中的记录；数据→排序和筛选→清除恢复它们。','高级筛选不要求先显示自动筛选下拉按钮，不能据此把两种筛选方式混为一谈。','自动筛选可从列标题下拉列表选职称等条件；清除条件后按钮保留，再次关闭“筛选”才移除按钮。','筛选不删除源记录；“复制到其他位置”输出的筛选副本不会因清除源区域条件自动消失。']});
  add('y2020q9','高级筛选有“在原有区域显示筛选结果”和“将筛选结果复制到其他位置”两种方式；条件区同一行表示AND，不同行表示OR。');
  add('y2026q26','Windows 10搜索可涉及本地文件、应用、设置及网络结果；具体网络结果受配置和连接影响，不能说搜索只能用于本地。');
  add('y2026q34','任务管理器可查看进程对应文件的属性、打开文件所在位置，并在“启动”页管理开机启动项；结束进程不等于卸载程序或删除文件。');
  add('merged-14','静态PNG保存一幅图像；APNG扩展支持多帧动画，已纳入W3C PNG第三版。不能将所有PNG都概括为不支持动画。');
  add('y2020q11','PowerPoint区分幻灯片母版、讲义母版和备注母版；普通演示文稿至少有一组幻灯片母版，可存在多组，版式数量并非永远固定。');
  add('y2020q28','防护应落实到操作：可疑邮件链接和附件先独立核实；及时安装官方安全更新；使用安全软件、独立备份与长且唯一的密码，支持时启用多因素认证。');
  add('y2020q41','基础入口：插入→页眉→编辑页眉，也可在打印布局中双击页面顶部；进入页眉后再输入文字或设置段落格式。');
  add('y2020q43','选中正文段落→打开段落对话框→特殊格式选“首行缩进”→度量值设“2字符”。首行缩进与整段左缩进、悬挂缩进控制的行不同。');
  add('y2020q58','按班级同时求三门课平均值：先按班级排序，分类字段选班级，汇总方式选平均值，同时勾选数学、英语、计算机。');
  add('y2020q26','时延是数据从发送端到接收端所需时间，包括发送、传播、处理和排队等部分；不能仅理解为排队等待。');
  add('y2020q24','文件是否能删除受占用方式、共享删除权限和访问权限等影响；被程序占用可能阻止删除，但并非所有打开的文件都绝对不能删除。');
  add('y2025q48','批量取消加粗：打开替换，填写查找内容及替换文字，定位“替换为”框→更多→格式→字体→字形选常规→确定→全部替换。');
  add('merged-7','标题要居中且无首行/悬挂偏移时，段落对齐设居中，特殊格式设“无”；只按居中不能保证去掉既有缩进。');
  add('y2026q25','也可从资源管理器把“此电脑”拖到桌面创建快捷方式，或通过发送到→桌面快捷方式创建普通项目入口；快捷方式与“桌面图标设置”恢复的系统图标要区分。');
  add('y2026q26','桌面右键→个性化→背景，可选择纯色、图片或幻灯片放映；背景设置不属于任务栏右键菜单的直接功能。');
  add('y2026q53','“更改图片”替换图片来源；“重设图片”重置图片的格式或裁剪等设置，不负责换成另一张源图片。');
  const relation=notes.find(n=>n.id==='merged-20');relation.points=relation.points.map(p=>p==='跨表取得关联信息通常先连接，再选择和投影。'?'跨表先连接，再按需选择行、投影列。若要所有学生的指定字段，不必额外做行条件筛选。':p);
  // Teaching text is textual HTML, never active document tags.
  for(const n of notes){n.keys=n.sources.map(s=>`${s.year}-${s.q}`);}
})();
