# v48 · 断线恢复检查

2026-09-07。先只读检查工作目录、项目结构、Git status、未暂存和暂存 diff、HEAD 提交及实际源码。初始工作区干净，HEAD 为 `1f396c85c780ee82f681596ab877a43db4455ce1`；不存在需要保留的未提交半成品。远端 main 的 v47 与本地 HEAD 的文件树相同（`749291d6ada696caba38fd7df2ef1d8bcf0908b9`）。

## 恢复时的分类

| 分类 | 代码与运行证据 |
| --- | --- |
| 已完成且正确 | 对比 v46，85 条原笔记已修订，增加 13 条考纲补充、16 条解析；共 233 条笔记、1042 个要点，460 个原题来源身份保留。17 个旧场景已迁入按章模型；组合键、左右键、统一目录、取消快速复习和展开操作区均已实现，完整回归通过。 |
| 已完成但需要修正 | 360px 展开操作区中文件夹标题与文件按钮在同一行排版，标题末字被挤至按钮旁；布局验收工具在章节加载未完成时可能返回“0 张卡片、0 问题”。 |
| 尚未完成 | 本次恢复的独立验收，以及上述两处局部修复；本轮均已完成。 |
| 无法确认 | 原卷扫描逐页核对、2026 年第 52/54 题完整题面、独立的 2027 完整考纲，以及真实 Windows/Office 与物理手机触屏表现。保留 v47 已明确标注的来源与教学模型边界。 |

## 最小修复

- `note-labs.css`：文件夹标题独占一行，短语和路径优先完整换行，极长文本仍可折行；文件按钮不再插入标题行。修改既有共享规则，无新增状态或覆盖式补丁。
- `tests/layout.html`：检查实际章节身份、加载状态与非空卡片集合；未加载完成时明确中断，不计为通过。首页没有卡片时给出提示；打开与测量复用同一保护。
- `site.config.json`、README 与生成物：更新为 v48，静态资源指纹同步。正文、题库、模型逻辑和用户数据未改写。

## 验收

- 恢复前代码与修复后 v48 分别执行完整 `npm run check`：均为 172 项通过、0 失败。项目没有独立 lint 或 TypeScript 类型检查脚本。
- 59 个 JS/CJS 文件语法通过；布局工具内联脚本语法通过。12 个页面的 209 个本地脚本/样式引用存在，构建校验正文、章节、演示注册、来源和关联路径。
- 初始完整桌面检查：11 章、1200px 框宽、233 张卡片，0 横向溢出与检测问题。修复后受影响的第 2 章在 360/390/1200px 再验，每次 25 张卡片，通过。
- 修复后的最终手机检查：11 章 × 360/390px，共 466 次卡片展开，0 横向溢出与检测问题。滚动条占宽使实际正文视口为 345/375px。
- 浏览器实际操作：Ctrl 与 Shift 同时选中后拖动生成快捷方式；返回保留选择和结果。右键模式实际拖动弹出菜单，选择复制后源文件保留、目标新增副本；Escape 可退出展开操作区。桌面与窄屏截图复查标题、按键和操作区；统一目录可打开。
- 自动化覆盖触屏长按、短按不误触、取消和收起后不延迟触发，以及实际指针松开时的组合键取值。没有将鼠标浏览器验收说成物理手机测试。
- 验收工具“未加载章节”和“首页无卡片”保护分别通过本地 DOM 检查与浏览器检查。加载期曾出现的零卡片结果已剔除并重跑。

窄屏溢出检查不能证明所有文案排版均美观，因此本次另做实际截图与交互复核。没有重写已通过的模型，也没有把考纲基线映射当作完整考纲覆盖率。
